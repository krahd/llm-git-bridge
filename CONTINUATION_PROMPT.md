Resume the Persistent Adversarial Executor job `shell-bridge-reliability-20260920-a1` in canonical repo `https://github.com/krahd/llm-git-bridge`. Read STATUS.md, PLAN.md, latest WORKLOG.md, then this file. Reconcile the workspace job and actual Git/remote state before mutation. Goal: harden the v5 shell bridge against the documented rclone transport stalls without changing raw-shell semantics or weakening Drive-ID pinning, replay/crash safety, or Git safety. Reuse the proven persistent `rclone rcd`/Unix-socket transport pattern already present in `src/llm_git_bridge/transport.py`; do not invent a second architecture unless exact code constraints require it. Work only through tiny bounded commands. Submit one bridge request at a time, check its exact result, use health once if pending, never resubmit an ambiguous mutation. Use the workspace coordinator for all edits/checkpoints. Add focused tests before implementation, then focused tests, shell_bridge subsets, and only then bounded full validation. Push checkpoints, integrate through the workspace gate, independently verify origin/main, then update the installed runtime and perform one controlled restart only when the live daemon is quiescent. Completion requires integrated/pushed main, verified deployed runtime, advancing health, a successful lightweight live request/result, and final adversarial audit. Continue autonomously until complete or a true external blocker.
