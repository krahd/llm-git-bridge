Resume Persistent Adversarial Executor job `shell-bridge-reliability-20260920-a1` in canonical repo `https://github.com/krahd/llm-git-bridge`. Read STATUS.md, PLAN.md, latest WORKLOG.md, then this file; reconcile workspace and actual Git/remote state before mutation. Goal is now one integrated reliability/concurrency system: remove shell-bridge transport starvation and make many simultaneous conversations safe by default. Preserve raw-shell semantics, pinned Drive IDs, replay/crash safety, GitHub canonical state, and unpublished local work. Reuse the proven persistent `rclone rcd` Unix-socket transport pattern already in the repo. For Git mutation, use one branch/worktree workspace per conversation/task, pushed checkpoints, logical resource + changed-path overlap detection, and serialize only the short integration gate. Keep read-only access cheap/concurrent. Work in microsteps: one substantive bridge request at a time, normally <=20 s, exact result check, health once if pending, never replay ambiguous mutations, no redundant full scans/tests. Add focused failing tests before implementation; checkpoint/push coherent phases; integrate through workspace gate; independently verify origin/main; deploy only after canonical verification at quiescence; controlled restart once; live concurrent acceptance; final adversarial audit. Continue autonomously until COMPLETE or a true external blocker.
