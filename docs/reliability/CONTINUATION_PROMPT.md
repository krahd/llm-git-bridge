Continue the `llm-git-bridge` reliability hardening from workspace job `bridge-reliability-20260920-a1` (resource `reliability/shell-bridge`) based on canonical commit `7ff2da2618f68dd9891f012b7006ce0ff2842895`. The established root incident is repeated rclone transport timeout bursts while the launchd-supervised daemon remains alive. v5 serializes poll, health, request download, result upload and delete behind one `_TRANSPORT_LOCK`, so queued worker transport can starve the control plane and make remote health look dead. Read `docs/reliability/PLAN.md`, `STATUS.md`, and `WORKLOG.md`, resume the existing workspace, add the deterministic starvation regression first, then implement transport priority/backpressure, progress-aware health/watchdog, installer/doctor improvements, adversarial tests, integration, GitHub verification and deployment. Preserve STARTED/FINISHED durability and never blindly replay or kill active work.
