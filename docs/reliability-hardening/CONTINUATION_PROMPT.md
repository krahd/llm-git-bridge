Resume the Shell Bridge reliability-hardening job. Read `docs/reliability-hardening/STATUS.md`, then `PLAN.md`, latest `WORKLOG.md`, and this file. Reconcile actual workspace/Git/remote/runtime state before mutation. Canonical repo is `krahd/llm-git-bridge`; GitHub is canonical, Mac checkout authoritative for local state, Drive is transport only. Resume existing workspace job `shell-bridge-reliability-20260920-a1`; do not create a replacement unless recovery proves it lost. The confirmed defect is serialized cold-rclone control-plane transport: repeated 30 s subprocess timeouts under a global lock starve polling/health while the daemon remains alive. Reuse the repository's proven private Unix-socket `rclone rcd` design while preserving shell protocol 1 and Drive root-folder-ID pinning. Work in timeout-safe microsteps, one outstanding shell-bridge request at a time. Never resubmit an ambiguous mutation; inspect reality first. Checkpoint coherent changes, test focused subsets before broader tests, integrate only through workspace validation, push/verify GitHub `main`, then deploy the installed runtime once at a quiescent point and live-test health/mailbox/logs. Continue without asking for permission. Completion requires canonical main verified, installed runtime verified, live probes green, state files updated to COMPLETE, and final adversarial audit passing.
